{
  "name": "makrell-playground",
  "version": "0.10.0",
  "private": true,
  "type": "module",
  "description": "Standalone MakrellTS browser playground for makrell.dev.",
  "scripts": {
    "build": "bun run build.mjs",
    "check": "bun run build"
  },
  "dependencies": {
    "monaco-editor": "^0.52.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "playwright": "^1.59.1",
    "typescript": "^5.6.3"
  }
}
